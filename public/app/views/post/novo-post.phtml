<!-- CONTAINER: CRIAR / EDITAR POST (ADMIN) -->
<form enctype="multipart/form-data" class="form-post mb-5" id="form-criar-post">

  <h2>Criar nova postagem</h2>

  <div class="form-floating mb-3">
    <select id="categoria" name="categoria" class="form-select">
      <option value=""> -- Selecione uma categoria --</option>
      <?php foreach ($this->view->categorias as $categoria): ?>
        <option value="<?= $categoria['id'] ?>"><?= $categoria['nome'] ?></option>
      <?php endforeach; ?>
    </select>
    <label for="categoria">Categoria</label>
  </div>

  <div class="form-floating mb-3">
    <input type="text" class="form-control" id="titulo" name="titulo" placeholder="Digite o título da postagem" value="">
    <label for="titulo">Título do post</label>
  </div>

  <div class="form-floating mb-3">
    <input type="text" class="form-control" id="slug" name="slug" placeholder="gerado-automaticamente" value="">
    <small>Use apenas letras minúsculas e hífen</small>
    <label for="slug">Slug (URL)</label>
  </div>

  <div class="row">
    <div class="col-md-4">
      <div class="form-floating mb-3">
        <select class="form-control" name="status" id="status">
          <option value="rascunho">Rascunho</option>
          <option value="publicado">Publicado</option>
        </select>
        <label for="status">Status</label>
      </div>
    </div>
    <div class="col-md-4">
      <div class="form-floating mb-3">
        <select class="form-control" name="status" id="status">
          <option value="0">Não</option>
          <option value="1">Sim</option>
        </select>
        <label for="status">Destaque Principal</label>
      </div>
    </div>
    <div class="col-md-4">
      <div class="form-floating mb-3">
        <select class="form-control" name="status" id="status">
          <option value="0">Não</option>
          <option value="1">Sim</option>
        </select>
        <label for="status">Destaque da Categoria</label>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
      <div class="input-group mb-3">
        <label class="input-group-text" for="capa">Imagem de capa</label>
        <input class="form-control" type="file" id="capa" name="capa" accept="image/*" value="">
      </div>
    </div>
  </div>

  <div class="form-floating mb-3">
    <textarea class="form-control" placeholder="Resumo curto para SEO e listagem" id="resumo" style="height: 100px"></textarea>
    <label for="resumo">Resumo</label>
  </div>

  <div class="form-floating mb-3">
    <textarea class="form-control" placeholder="Conteudo" id="conteudo" style="height: 250px"></textarea>
    <label for="conteudo">Conteúdo</label>
  </div>

  <div class="d-grid gap-2 d-md-flex justify-content-md-end">
    <button type="reset" class="btn btn-warning me-3">Limpar</button>
    <button type="submit" name="acao" value="publicar" class="btn btn-primary">Avançar</button>
  </div>

</form>

<script>
  // Função para gerar slug a partir do título
  function gerarSlug(texto) {
    return texto
      .toLowerCase()
      .normalize('NFD').replace(/[\u0300-\u036f]/g, '')
      .replace(/[^a-z0-9 ]/g, '')
      .trim()
      .replace(/\s+/g, '-');
  }

  const tituloInput = document.getElementById('titulo');
  const slugInput = document.getElementById('slug');
  let slugEditado = false;

  slugInput.addEventListener('input', () => slugEditado = true);

  tituloInput.addEventListener('input', () => {
    if (!slugEditado) {
      slugInput.value = gerarSlug(tituloInput.value);
    }
  });
</script>