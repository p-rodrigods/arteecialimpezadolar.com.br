<!-- CONTAINER: CRIAR / EDITAR POST (ADMIN) -->
<form enctype="multipart/form-data" class="form-post mb-5" id="form-edit-post">
  <h2>Editar Postagem</h2>
  <div>
    <input type="hidden" name="id" id="id" value="<?= $this->view->post['id'] ?>">
  </div>
  <div class="form-floating mb-3">
    <select id="categoria" name="categoria" class="form-select">
      <option > -- Selecione uma categoria --</option>
      <?php foreach ($this->view->categorias as $categoria): ?>
         <option value="<?= $categoria['id'] ?>"
        <?= (isset($this->view->post['categoria_id']) && (int)$this->view->post['categoria_id'] === (int)$categoria['id']) ? 'selected' : '' ?>>
        <?= $categoria['nome'] ?>
    </option>
      <?php endforeach; ?>
    </select>
    <label for="categoria">Categoria</label>
  </div>

  <div class="form-floating mb-3">
    <input type="text" class="form-control" id="titulo" name="titulo" placeholder="Digite o título da postagem" value="<?= $this->view->post['titulo'] ?>">
    <label for="titulo">Título do post</label>
  </div>

  <div class="form-floating mb-3">
    <input type="text" class="form-control" id="slug" name="slug" placeholder="gerado-automaticamente" value="<?= $this->view->post['slug'] ?>">
    <small>Use apenas letras minúsculas e hífen</small>
    <label for="slug">Slug (URL)</label>
  </div>

  <div class="row">
    <div class="col-md-4">
      <div class="form-floating mb-3">
        <select class="form-control" name="status" id="status">
          <option value="rascunho" <?= ($this->view->post['status'] === 'rascunho') ? "selected": '' ?>>Rascunho</option>
          <option value="publicado" <?= ($this->view->post['status'] === 'publicado') ? 'selected' : '' ?>>Publicado</option>
        </select>
        <label for="status">Status</label>
      </div>
    </div>
    <div class="col-md-4">
      <div class="form-floating mb-3">
        <select class="form-control" name="destaque_principal" id="dPrincipal">
          <option value="0" <?=  ($this->view->post['destaque_principal'] == 0 ) ? 'selected' : '' ?> >Não</option>
          <option value="1"  <?=  ($this->view->post['destaque_principal'] == 1 ) ? 'selected' : '' ?> >Sim</option>
        </select>
        <label for="status">Destaque Principal</label>
      </div>
    </div>
    <div class="col-md-4">
      <div class="form-floating mb-3">
        <select class="form-control" name="destaque_categoria" id="dCategoria">
          <option value="0"  <?=  ($this->view->post['destaque_categoria'] == 0 ) ? 'selected' : '' ?>>Não</option>
          <option value="1"  <?=  ($this->view->post['destaque_categoria'] == 1 ) ? 'selected' : '' ?>>Sim</option>
        </select>
        <label for="status">Destaque da Categoria</label>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
         <div class="input-group mb-3">
         <label class="input-group-text" for="capa">Imagem de capa</label>
        <input class="form-control" type="file" id="capa" name="capa" accept="image/*" value="<?= $this->view->post['imagem'] ?>">
      </div>
    </div>
  </div>

  <div class="form-floating mb-3">
    <textarea class="form-control" id="resumo" name="resumo" style="height: 50px" placeholder="Resumo curto para SEO e listagem"> <?= $this->view->post['resumo'] ?> </textarea>
      <label for="resumo">Resumo</label>
  </div>

  <div class="form-floating mb-3">
    <textarea class="form-control" id="conteudo" name="conteudo" style="height: 250px"><?= $this->view->post['conteudo'] ?></textarea>
    <label for="conteudo">Conteúdo</label>
  </div>

 <div class="d-grid gap-2 d-md-flex justify-content-md-end">
    <button type="submit" name="acao" value="publicar" class="btn btn-primary">Atualizar</button>
  </div>

</form>

<script>
  // Função para gerar slug a partir do título
  function gerarSlug(texto) {
    return texto
      .toLowerCase()
      .normalize('NFD').replace(/[\u0300-\u036f]/g, '')
      .replace(/[^a-z0-9 ]/g, '')
      .trim()
      .replace(/\s+/g, '-');
  }

  const tituloInput = document.getElementById('titulo');
  const slugInput = document.getElementById('slug');
  let slugEditado = false;

  slugInput.addEventListener('input', () => slugEditado = true);

  tituloInput.addEventListener('input', () => {
    if (!slugEditado) {
      slugInput.value = gerarSlug(tituloInput.value);
    }
  });

</script>